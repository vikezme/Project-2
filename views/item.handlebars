<div class="container">
    <div class="row">
        <div class="col s12">
            <div class="card">
                <div class="card-content text-black">
                <span class="card-title center-align">{{editedItem.title}}</span>
                <p>{{editedItem.description}}</p>
              
                </div>
                <div class="divider"></div>
            
                <div class="card-action ">
                <p>Posted by : <span>{{editedItem.user.username}}</span> </p>
                <a class="btn-floating btn-large waves-effect waves-light red accent-2 right-align"><i class="material-icons">save</i></a> 
                </div>
            </div>
        </div>
          
          <div class="col s12 ">
            <!---->
            <form>
              <div class="input-field">
                  <input type="email" class="validate" id ="email">
                  <label for="email" class="active">Your email address: </label>
              </div>

              <div class="input-field">
                  <textarea type="textarea" id="contactMsg" class="materialize-textarea"></textarea>
                  <label for="contactMsg" class="active">Write your message here:</label>
              </div>


              <div class="center-align">
                  <a class="red accent-2 waves-effect btn" href="/search">Back</a>
                  <a class="red accent-2 waves-effect btn" href="">Send Message</a>
              </div>
          </form>
            <!---->
          </div>







      </div>